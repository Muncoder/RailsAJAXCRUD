<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @starship.name %>
</p>

<p>
	<h2>Crew Members</h2>
	<ul>
		<% @starship.crew_members.each do |crew| %>
			<li><%= crew.name %> ---<%= crew.division %></li>
		<% end %>
	</ul>
</p>

<%= link_to 'Edit', edit_starship_path(@starship) %> |
<%= link_to 'Back', starships_path %>
